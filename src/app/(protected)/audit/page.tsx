import Link from "next/link";
import { requireActor } from "@/lib/auth/authorization";
import { jakartaDate } from "@/lib/time/business-date";
import { listAuditEvents } from "@/modules/audit/audit-query.service";
const valid=(v:string|undefined,f:string)=>/^\d{4}-\d{2}-\d{2}$/.test(v??"")?v!:f;
export default async function AuditPage({searchParams}:{searchParams:Promise<{from?:string;to?:string;module?:string;action?:string}>}){const actor=await requireActor();const q=await searchParams;const today=jakartaDate(),from=valid(q.from,today),to=valid(q.to,today);const events=await listAuditEvents(actor,{from,to,module:q.module||undefined,action:q.action||undefined}) as Array<Record<string,unknown>>;return <main className="page-shell"><header className="page-header"><div><h1>Audit Trail</h1><p className="muted">Append-only material event history. No edit/delete controls are provided.</p></div><form className="date-filter"><label>From<input type="date" name="from" defaultValue={from}/></label><label>To<input type="date" name="to" defaultValue={to}/></label><label>Module<input name="module" defaultValue={q.module??""} placeholder="FINANCE"/></label><label>Action<input name="action" defaultValue={q.action??""} placeholder="UPDATE"/></label><button className="primary-button">Filter</button></form></header><section className="panel"><div className="table-wrap"><table className="data-table"><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Module</th><th>Entity</th><th>Reason</th><th></th></tr></thead><tbody>{events.map(e=><tr key={String(e.id)}><td>{new Date(String(e.created_at)).toLocaleString("id-ID")}</td><td>{String(e.actor_name)}<br/><small>{String(e.actor_role??"SYSTEM")}</small></td><td>{String(e.action)}</td><td>{String(e.module)}</td><td>{String(e.entity_type)}<br/><small>{String(e.entity_id)}</small></td><td>{String(e.reason??"-")}</td><td><Link href={`/audit/${e.id}`}>Detail</Link></td></tr>)}</tbody></table></div></section></main>}
