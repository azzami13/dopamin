export default function Loading() { return <div className="page-shell loading-state" role="status" aria-live="polite"><p>Memuat workspace...</p><div className="skeleton skeleton-title"/><div className="kpi-grid">{[1,2,3,4].map(i => <div key={i} className="skeleton skeleton-card"/>)}</div><div className="skeleton skeleton-panel"/><span className="sr-only">Mohon tunggu hingga data selesai dimuat.</span></div>; }
