import { requirePermission } from "@/lib/auth/authorization";import { Permission } from "@/lib/auth/permissions";import { getDailyReportRun } from "@/modules/reporting/daily-report.service";
export default async function ReportDetail({params}:{params:Promise<{id:string}>}){const actor=await requirePermission(Permission.REPORT_VIEW);const {id}=await params;const d=await getDailyReportRun(actor,id);return <main className="page-shell"><header><h1>Report {d.run.businessDate} — R{d.run.revisionNo}</h1><p className="muted">{d.run.triggerType} • {d.run.completenessStatus} • {d.run.status}</p></header><div className="dashboard-grid"><div className="panel"><h2>Metrics Snapshot</h2><pre className="json-view">{JSON.stringify(d.run.metricsSnapshot,null,2)}</pre><h3>Missing Sources</h3><pre className="json-view">{JSON.stringify(d.run.missingSources,null,2)}</pre></div><div className="panel"><h2>Deliveries</h2>{d.deliveries.map(x=><div key={x.id} className="delivery-row"><strong>{x.recipientEmail}</strong><span>{x.deliveryStatus}</span><small>{x.sentAt?.toLocaleString('id-ID')??'-'}</small>{x.errorMessage&&<span className="error-text">{x.errorMessage}</span>}</div>)}</div></div></main>}
